export interface PagionationProps {
    currentPage: number | 1,
    prevPage: number,
    nextPage: number
}
export interface PaginateProps {
    currentPage: number | 1,
    prevPage: number,
    nextPage: number
}