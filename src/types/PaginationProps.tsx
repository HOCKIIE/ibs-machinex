export interface PagionationType {
    currentPage: number | 1,
    prevPage: number,
    nextPage: number
}
export interface PaginateType {
    currentPage: number | 1,
    prevPage: number,
    nextPage: number
}